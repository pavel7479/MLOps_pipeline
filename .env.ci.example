# Public, disposable CI-only values. These are not production credentials.
POSTGRES_DB=ci_db
POSTGRES_USER=ci_user
POSTGRES_PASSWORD=ci_password_disposable_only
API_HOST_PORT=28000
MLFLOW_HOST_PORT=25000
REGISTRY_BOOTSTRAP_PROFILE=ci-fixture
